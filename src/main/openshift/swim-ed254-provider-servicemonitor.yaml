apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: swim-ed254-provider-metrics
  labels:
    app: swim-ed254-provider
    app.kubernetes.io/name: swim-ed254-provider
    app.kubernetes.io/part-of: swim-operator
spec:
  selector:
    matchLabels:
      app: swim-ed254-provider
  endpoints:
  - port: http
    path: /q/metrics
    interval: 30s
    scrapeTimeout: 10s
